# ==================================================
# NEXUS v2.0 - Environment Configuration
# ==================================================
# ИНСТРУКЦИЯ:
# 1. Скопируй этот файл как `.env` в корень проекта
# 2. Заполни API ключи (см. ниже где взять)
# 3. Запусти: docker-compose up -d
# ==================================================

# ---------------- DATABASE ----------------
POSTGRES_USER=nexus
POSTGRES_PASSWORD=nexus_secure_password_123
POSTGRES_DB=nexus
DATABASE_URL=postgresql://nexus:nexus_secure_password_123@postgres:5432/nexus

# ---------------- REDIS ----------------
REDIS_URL=redis://redis:6379/0

# ---------------- MINIO (S3 Storage) ----------------
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_ENDPOINT=minio:9000
MINIO_BUCKET=nexus-media
MINIO_SECURE=false
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# ---------------- API KEYS ----------------
# 🔑 ОБЯЗАТЕЛЬНО: Gemini API Key
# Где взять: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# 🔑 ОПЦИОНАЛЬНО: OpenAI API Key (fallback для Whisper API)
# Если не указан - будет использоваться faster-whisper локально
# Где взять: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# ---------------- API SETTINGS ----------------
API_HOST=0.0.0.0
API_PORT=8000
API_PREFIX=/api
DEBUG=false

# CORS (разреши фронтенд)
CORS_ORIGINS=["http://localhost:5173","http://localhost:8000"]

# ---------------- WHISPER SETTINGS ----------------
# Модель: tiny, base, small, medium, large-v2, large-v3
# Рекомендую: small (быстро + качество) или medium (лучше качество)
WHISPER_MODEL=small
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8

# ---------------- GEMINI SETTINGS ----------------
GEMINI_MODEL=gemini-2.0-flash-exp
VECTOR_DIMENSION=768

# ---------------- CELERY SETTINGS ----------------
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# ---------------- LOGGING ----------------
LOG_LEVEL=INFO
