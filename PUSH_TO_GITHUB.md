# Как залить на GitHub

Проект подготовлен и закоммичен. Теперь осталось только запушить.

## Вариант 1: Если репозиторий пустой или ты хочешь заменить всё

```bash
cd /path/to/Nexus  # твой путь к проекту
git push -u origin main --force
```

`--force` перезапишет всё что там есть.

## Вариант 2: Если в репозитории уже есть важные файлы

```bash
cd /path/to/Nexus
git pull origin main --rebase  # подтянет существующие файлы
git push -u origin main
```

## Проблемы с авторизацией?

Если GitHub просит логин/пароль:

### Используй Personal Access Token (PAT):

1. Иди на https://github.com/settings/tokens
2. Generate new token (classic)
3. Выбери scope: `repo` (полный доступ к приватным репозиториям)
4. Скопируй токен

Теперь вместо пароля используй этот токен:

```bash
git push -u origin main
# Username: твой_github_username
# Password: вставь_токен_сюда
```

### Или настрой SSH:

```bash
# Смени URL на SSH
git remote set-url origin git@github.com:kitgriului/nexus-chat.git

# Добавь SSH ключ на GitHub (если ещё не добавлен)
# https://github.com/settings/keys
```

---

## После успешного пуша

Проект будет доступен на:
https://github.com/kitgriului/nexus-chat

Можешь сразу проверить:
- README отображается на главной
- Структура папок правильная
- Все файлы на месте

---

Всё готово! 🚀
